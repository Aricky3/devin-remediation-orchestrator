services:
  orchestrator:
    build: .
    image: devin-remediation-orchestrator:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - orchestrator-data:/app/data
    restart: unless-stopped

volumes:
  orchestrator-data:
